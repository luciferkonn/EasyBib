Invoke the `easybib` skill in **create** mode.

Read `SKILL.md` and follow the "/create" workflow. Arguments: $ARGUMENTS.

If $ARGUMENTS contains "dry run" or "--dry-run", run in dry-run mode (no writes).
