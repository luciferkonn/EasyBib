Invoke the `easybib` skill in **organize** mode.

Read `SKILL.md` and follow the "/organize" workflow. Arguments: $ARGUMENTS.

If $ARGUMENTS contains "dry run" or "--dry-run", run in dry-run mode (no writes).
